#include "main.h"
#include <stdio.h>

/**
 * main - argc & argv function
 * @argc: total argument
 * @argv: the arguments
 * Return: Always 0(Success)
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
